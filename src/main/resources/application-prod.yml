
spring:
  application:
    name: msa-discovery
server:
  port: 8761
eureka:
  instance:
    hostname: 54.87.40.18
  client:
    register-With-eureka: true
    fetch-Registry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/